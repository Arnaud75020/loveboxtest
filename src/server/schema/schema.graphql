scalar JSON
scalar Date

type Message {
  content: String
}

type Query {
  "Get all messages"
  getAllMessages: Message  
  "Get new message"
  getNewMessage: Message  
}

type Mutation {
  "Set new message"
  setNewMessage(content: String!): Message
}
