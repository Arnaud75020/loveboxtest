scalar JSON
scalar LocalDateTime

type Message {
  msg: String
  date: LocalDateTime
}

type Query {
  "Get new message"
  getMessage: [Message]
}

type Mutation {
  "Send message"
  sendMessage(msg: String): Message!
}


