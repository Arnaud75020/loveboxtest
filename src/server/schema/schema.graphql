scalar JSON
scalar LocalDateTime

type Message {
  id: Int!
  time: LocalDateTime
  msg: String
}


type Query {
  "Get messages"
  getMessages: [Message]
}

type Mutation {
  "Send message"
  sendMessage(msg: String): Message!
}


